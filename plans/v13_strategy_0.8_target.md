# v13戦略: 0.8超えを目指す最終戦略

## 現状分析

### 現在のベスト: v5.2 = 0.77702
- LR: 5e-6, r=64, α=128, dropout=0, epochs=1
- データ: v5 (3,869件、XMLエラー除去済み)

### 過去の失敗実験
| バージョン | 設定 | スコア | 問題点 |
|-----------|------|--------|--------|
| v12.1 | NEFTune(α=5) | 0.6912 | 説明文追加で形式破壊 |

---

## Person Q以降の重要な発見

### 🏆 Person T (パース成功率 96.7%)
**衝撃の発見**: TOMLデータを削除してSFT学習した結果、全体で96.7%のパース成功率を達成

- **TOML出力のデータはTOMLの文法学習にまったく寄与していない**
- 「TOMLはTOMLデータでは学習していない」
- フォーマット別結果:
  - CSV: 100.0%
  - JSON: 100.0%
  - TOML: 84.0% (fail 4) ← TOML除去したのに84%！
  - XML: 95.0%
  - YAML: 100.0%

### 🥇 Person U (LB 0.84達成)
**最も参考にすべき成功例**

#### 戦略の核心:
1. **ベースモデルに近い方が安定**: 学習しすぎると読解力が落ちる
2. **Sequential SFT**:
   - ベースモデルで特定フォーマットを学習
   - そのモデルをベースに別フォーマットを学習
3. **パラメータ凸探索**: 各パラメータを変えていくと「凸の頂点」がある
4. **フォーマット別限界点の発見**: 各データセットで文法正解率の限界点を求めた

#### 結果:
- CSV: 100%、JSON: 100%、YAML: 100%
- TOML: 92% (fail 2)
- XML: 95% (fail 1)

### 🥈 Person W (LB 0.8以上)
- **質 > 量**: 最終的には1000件以下のデータで学習
- **微調整が重要**: 大きな変更より微調整で変化を確認
- 基準LLMの性能が良い場合は、細かい調整でスコアを上げられる

### 🥉 Person Z (LB 0.82以上)
- SFTのパラメータチューニングのみで0.82達成
- **コンテキスト長の拡張**が0.7の壁を超える第一歩
- **過学習の抑制**が高スコアの鍵
- **学習率の極微細なチューニング**でスイートスポットを発見

### Person AA (LB 0.8以上)
- **rsLoRA推し**: `use_rslora=True` が効果的
- 過学習防止パラメータは大きめが効いた
- 低めのlearning rateが有効

---

## v13戦略: 複合アプローチ

### Phase 1: v13.0 - TOML出力除去版
**目的**: Person Tの発見を検証

- **データ**: v5からTOML出力サンプルを除去（3,258件）
- **パラメータ**: v5.2と同じ（LR=5e-6, r=64, α=128）
- **期待**: TOML除去が全体精度に好影響を与えるか確認

### Phase 2: v13.1 - 低学習率版
**目的**: Person U/Z/AAの「低学習率が有効」を検証

- **データ**: v13（TOML除去版、3,258件）
- **パラメータ**: LR=3e-6（v5.2の60%）、他は維持
- **仮説**: 低学習率でベースモデルの能力を維持

### Phase 3: v13.2 - rsLoRA + 過学習防止版
**目的**: Person AAのrsLoRA推奨を検証

- **データ**: v13（3,258件）
- **パラメータ**:
  - LR=3e-6
  - r=128, α=128
  - `use_rslora=True`
  - dropout=0.05
  - weight_decay=0.1

### Phase 4: v14 - Sequential SFT（Person U戦略の再現）
**目的**: 最も効果的だったPerson Uの戦略を完全再現

1. **Stage 1**: JSON/YAML特化学習
2. **Stage 2**: Stage 1のモデルでXML学習
3. **Stage 3**: Stage 2のモデルでTOML学習
4. **Stage 4**: 全フォーマットの統合微調整

---

## 作成済みリソース

### データセット
- `inputs/sft_processed/v13/train.json` (3,258件)
  - JSON: 1,075件
  - YAML: 624件
  - CSV: 547件
  - XML: 1,012件
  - TOML: 0件（除去済み）

### ノートブック（要作成）
1. `v13.0_toml_removed.ipynb` - TOML除去版ベースライン
2. `v13.1_low_lr.ipynb` - 低学習率版
3. `v13.2_rslora.ipynb` - rsLoRA + 過学習防止版

---

## 推奨実行順序

```
1. v13.0 (TOML除去 + v5.2パラメータ) → ベースライン確認
   ↓
2. v13.1 (LR=3e-6) → 低学習率の効果確認
   ↓
3. v13.2 (rsLoRA + 過学習防止) → rsLoRAの効果確認
   ↓
4. 最も良い結果を元にパラメータ凸探索
   ↓
5. v14 (Sequential SFT) → Person U戦略の完全再現
```

---

## 成功の鍵（まとめ）

1. **TOML出力データは除去**: 学習に寄与していない
2. **低学習率**: 5e-6より低く（3e-6〜1e-6）
3. **rsLoRA**: `use_rslora=True` で効果的な学習
4. **過学習防止**: dropout、weight_decayを適切に設定
5. **Sequential SFT**: フォーマット別の順次学習
6. **質 > 量**: 1000件以下でも0.8超え可能
7. **パラメータ凸探索**: 微調整で最適点を発見

---

## 期待スコア

- v13.0: 0.78〜0.80（TOML除去の効果）
- v13.1: 0.79〜0.81（低学習率の効果）
- v13.2: 0.80〜0.82（rsLoRAの効果）
- v14: 0.82〜0.84（Sequential SFTの効果）

**目標: 0.80以上達成**
